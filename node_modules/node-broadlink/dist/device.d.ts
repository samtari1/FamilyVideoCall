/// <reference types="node" />
/// <reference types="node" />
import { RemoteInfo } from 'dgram';
export default class Device {
    readonly host: RemoteInfo;
    readonly mac: number[];
    readonly deviceType: number;
    readonly model: string;
    readonly manufacturer: string;
    readonly name: string;
    readonly isLocked: boolean;
    protected TYPE: string;
    private id;
    private key;
    private count;
    private readonly iv;
    private readonly socket;
    constructor(host: RemoteInfo, mac: number[], deviceType?: number, model?: string, manufacturer?: string, name?: string, isLocked?: boolean);
    private encrypt;
    protected decrypt(response: Buffer): Buffer;
    auth(): Promise<Device>;
    protected sendPacket(buffer: Buffer, command?: number): Promise<Buffer>;
    protected sendPacketAndDecrypt(buffer: Buffer): Promise<Buffer>;
}
