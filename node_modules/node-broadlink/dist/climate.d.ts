import Device from './device';
interface DayModel {
    startHour: number;
    startMinute: number;
    temp: number;
}
export declare class Hysen extends Device {
    protected TYPE: string;
    private calculateCrc16;
    private static toBytes2;
    private sendRequest;
    getTemp(): Promise<number>;
    getExternalTemp(): Promise<number>;
    getFullStatus(): Promise<{
        remoteLock: number;
        power: number;
        active: number;
        tempManual: number;
        roomTemp: number;
        thermostatTemp: number;
        autoMode: number;
        loopMode: number;
        sensor: number;
        osv: number;
        dif: number;
        svh: number;
        svl: number;
        roomTempAdj: number;
        fre: number;
        poweron: number;
        unknown: number;
        externalTemp: number;
        hour: number;
        min: number;
        sec: number;
        dayofweek: number;
        weekDay: DayModel[];
        weekEnd: DayModel[];
    }>;
    setMode(autoMode: number, loopMode: number, sensor?: number): Promise<void>;
    setAdvanced(loopMode: number, sensor: number, osv: number, dif: number, svh: number, svl: number, adj: number, fre: number, powerOn: number): Promise<void>;
    setTemp(temp: number): Promise<void>;
    setPower(power?: number, remoteLock?: number): Promise<void>;
    setTime(hour: number, minute: number, second: number, day: number): Promise<void>;
    setSchedule(weekday: DayModel[], weekend: DayModel[]): Promise<void>;
}
export {};
