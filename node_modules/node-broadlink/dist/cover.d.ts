import Device from './device';
export declare class Dooya extends Device {
    TYPE: string;
    open(): Promise<number>;
    close(): Promise<number>;
    stop(): Promise<number>;
    getPercentage(): Promise<number>;
    setPercentageAndWait(newPercentage: number): Promise<number>;
    private send;
}
